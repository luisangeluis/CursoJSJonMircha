//Indefinido es cuando inicializas una variable pero no tiene asignacion.
let indefinada;
console.log(indefinada);

//Nulo es cuando una variable la asignas un valor vacio
let nulo = null;
console.log(nulo);

//Nan Not A Number sale cuando intentas hacer operaciones no validas
let result = 1 * 'hola';
console.log(result);